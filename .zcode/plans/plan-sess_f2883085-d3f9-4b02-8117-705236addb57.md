# OJ 项目架构分析与无关内容识别 — 交付计划

## 已完成的分析（只读扫描，3 个并行探索代理）

已对 4 个子项目完成「模块级 + 关键文件」深度扫描：

1. **oj-backend-master（单体后端）**：Spring Boot 2.7.2 单模块应用，HTTP 调用沙箱（8090 端口），策略模式判题。无关内容：Post 帖子全家桶（Controller/Service/Mapper/DTO/VO）、ES 同步任务、微信公众号接入、COS 文件上传、freemarker/easyexcel 未用依赖、sql 中 3 张帖子表等。
2. **oj-backend-microservice-master（微服务后端）**：Spring Cloud Alibaba，Gateway(8101) + user(8102)/question(8103)/judge(8104)，OpenFeign + RabbitMQ + Nacos。无关内容：Post 实体/VO、mysql-init 中 3 张帖子表、UploadFileRequest/FileConstant(COS)、NetUtils、ThirdPartyCodeSandbox、InitRabbitMq 死代码、easyexcel/sentinel/gson 未用依赖。
3. **oj-code-sandbox-master（代码沙箱）**：Spring Boot 2.7.14，Native/Docker 双实现（Docker 未被引用）。无关内容：security 安全管理器教学包（含 .class）、unsafe 攻击示例包、两个 *Old 废弃类、DockerDemo、tmpCode 运行残留、「木马程序.bat」。
4. **oj-frontend-master（前端）**：Vue CLI 5（webpack，非 Vite）+ Vue3 + Vuex + Arco。无关内容：AboutView/AdminView/ExampleView/HelloWorld 废弃页组件、generated/ 中 Post*/File/WxMp 共 5 个无关服务、伪造管理员身份调试代码（GlobalHeader.vue:69-74）、querystring 死导入。

另识别出硬编码弱口令/密钥（root/123456、SALT="yupi"、明文 secretKey、CORS 全放开）作为配置风险项单独列出。

## 交付动作

1. **写入报告文件** `D:\zcode_work\shanxi-Oj\ANALYSIS.md`，结构如下：
   - 按四个子项目分节：目录结构与架构设计 → 模块职责边界 → 依赖/调用关系与数据流 → 技术栈 → 无关内容逐项清单（含文件路径、代码位置、判定理由、依据置信度）
   - 每项无关内容标注分类：**可安全删除**（构建产物、未被任何引用的死代码、教学演示包）/ **存在删除风险**（如 ExampleCodeSandbox 被工厂 default 分支引用、docker-java 依赖支撑 Docker 沙箱实现、easyexcel 需连带处理测试类）/ **待确认**（如单体后端 easyexcel 仅测试引用是否保留、前端注册页占位是否为待实现功能）
   - 结尾汇总：无关内容总清单表 + 删除影响评估（对编译、启动、核心判题流程的影响逐项说明）
2. **聊天中同步输出**完整分析结论（与报告内容一致）

## 明确不做的事

- 不删除任何文件、不修改任何代码（本任务仅为分析评估，用户未要求执行清理）